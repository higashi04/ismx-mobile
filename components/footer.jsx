import { StyleSheet, Text } from "react-native";

export default function Footer() {
    return(
        <Text style={styles.text}>ISMX :B</Text>
    )
}

const styles = StyleSheet.create({
    text: {
        fontSize: 15,
        backgroundColor: 'olive',
    }
})
